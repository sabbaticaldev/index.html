{
  "name": "scaffold",
  "type": "module",
  "version": "0.1.0",
  "main": "src/index.mjs",
  "module": "src/index.mjs",
  "scripts": {
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "build": "tsc",
    "watch": "tsc -w",
    "preversion": "npm run build",
    "postversion": "git push --follow-tags"
  },
  "files": [
    "src",
    "dist"
  ],
  "dependencies": {
    "backend": "workspace:^",
    "shared": "^0.2.0",
    "uix": "workspace:^"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  },
  "peerDependencies": {
    "lit": "^3.0.0"
  }
}
